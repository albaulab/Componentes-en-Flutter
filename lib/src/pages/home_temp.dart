import 'package:flutter/cupertino.dart';

class HomePagesTemps extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Home Temporal"),
    );
  }
}
